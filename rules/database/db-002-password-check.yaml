id: db-002
name: "密碼不應 hardcode"
enabled: true
severity: error

targets:
  file_patterns:
    - "**/db*.yaml"

rule:
  type: pattern_match
  path: "database.password"
  pattern: '^\$\{.*\}$'
  message: "password 必須使用環境變數，格式: ${VAR_NAME}"
